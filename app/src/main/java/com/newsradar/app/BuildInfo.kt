package com.newsradar.app

/**
 * Monotonic build number, bumped by +1 on every compile/push. Shown in the feed
 * top bar so it's easy to confirm which APK is installed. Current: 102.
 */
object BuildInfo {
    const val BUILD_NUMBER = 102
}
